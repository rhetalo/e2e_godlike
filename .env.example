# Copy this file to .env and fill in your values
# Never commit .env to git

# VirtFusion panel credentials (test account)
PANEL_EMAIL=test@testmail.com
PANEL_PASSWORD=your_password_here

# Password used when auto-generating test user registrations (registration flow tests)
TEST_USER_PASSWORD=Password_123
